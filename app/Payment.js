const { con } = require("../model/DB")

class PaymentLogic {
    async findOne(authority) {
        return new Promise((resolve, reject) => {
            
        });
    }
}

module.exports = new PaymentLogic()